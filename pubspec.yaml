name: squash
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# --- CORRECT MAIN DEPENDENCIES BLOCK ---
dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.0.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.1.1

  # ADDED: Package required for making API calls to scoring service
  http: ^1.2.1
  
  # Firebase packages
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.2
  google_sign_in: ^6.2.1
  # Code editor & syntax highlight
  code_text_field: ^1.1.0
  highlight: ^0.7.0
  
  # Push notifications
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# The rest of the file remains structural
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - _img/iconSq.png
    - _img/iconSqTEXT.png
# --- END OF FILE ---

